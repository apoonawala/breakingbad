<h1>The Beginning of Breaking Bad!</h1>
<p><%="Welcome #{current_user.email}"%></p>

<%=button_to "Create a new habit", new_habit_url, method: "get"%>

<h4>Here are your current habits:</h4>
  <ul>
    <% @habits.each do |h|%>
      <li><%=link_to"Name: #{h.name}", habit_url(h.id) %></li>
    <%end%>
  </ul>
